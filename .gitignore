# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Trained weights (regenerate with `python train.py`)
reports/model.pt
# PowerShell redirect log (UTF-16); the sample/metrics/figure are committed instead
reports/train_log.txt

# Frontend deps + build log
web/node_modules/
web/.vite/
web_build.log

# The corpus (data/input.txt), report figures/sample, and web_model/gpt.pt
# (the committed weights for the web playground) ARE committed.
